<%= render(HeaderComponent.new(
  title: "Todo"
)) do |c| %>
  <%= c.with_crumb("Home", home_path) %>
<% end %>

<div class="flex space-x-5">
  <%= link_to(
    "Tasks",
    todo_tasks_path,
    class: "button"
  ) %>

  <%= link_to(
    "Rooms",
    todo_rooms_path,
    class: "button"
  ) %>
</div>
