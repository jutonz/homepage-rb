<div class="flex flex-col gap-4 mb-20">
  <div class="grid gap-4 grid-cols-2 sm:grid-cols-5">
    <%= render(partial: "galleries/image", collection: images) %>
    <div class="hidden only:flex flex-row">
      No images.
    </div>
  </div>

  <div class="flex justify-center">
    <%= paginate images %>
  </div>
</div>
