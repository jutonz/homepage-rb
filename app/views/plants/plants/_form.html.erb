<%= simple_form_for(
  @plant,
  url: @plant.persisted? ? plant_path(@plant) : plants_path
) do |form| %>
  <%= form.input(:name, autofocus: true) %>
  <%= form.input(:purchased_from) %>
  <%= form.input(:purchased_at, as: :date, html5: true) %>
  <%= form.button(:submit) %>
<% end %>
