<% flash.each do |flash_type, message| %>
  <div class="flex gap-y-3 absolute bottom-20 left-10">
    <%= render(FlashComponent.new(
      message:,
      type: flash_type
    )) %>
  </div>
<% end %>
