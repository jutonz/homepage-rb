<%= render(HeaderComponent.new(
  title: "Edit Recipe Group"
)) do |c| %>
  <%= c.with_crumb("Home", home_path) %>
  <%= c.with_crumb("Recipe Groups", recipe_groups_path) %>
  <%= c.with_crumb(@recipe_group.name, recipe_group_path(@recipe_group)) %>
<% end %>

<%= render "form", recipe_group: @recipe_group %>