<%= simple_form_for(@ingredient, url: @ingredient.persisted? ? ingredient_path(@ingredient) : ingredients_path) do |form| %>
  <%= form.input(:name, autofocus: true) %>
  <%= form.button(:submit) %>
<% end %>
